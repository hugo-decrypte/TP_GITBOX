{% extends "categories.html.twig" %}

{% block details %}
    <div class="mt-8 bg-white shadow rounded p-6">
        <h1 class="text-2xl font-bold text-purple-700 mb-4">{{ category.libelle|upper }}</h1>

        <h3 class="text-lg font-semibold text-gray-700 mb-2">Description :</h3>
        <p class="text-gray-600">{{ category.description|default('Aucune description disponible.')|e }}</p>

        <br>
        {% block button %}
            <a class="px-3 py-1 bg-purple-100 text-purple-800 rounded hover:bg-purple-200 transition" href="/categories/{{ category.id }}/prestations"> Voir les prestations de cette catégorie </a>
        {% endblock %}
    </div>

    {% block prestations %}{% endblock %}
{% endblock %}